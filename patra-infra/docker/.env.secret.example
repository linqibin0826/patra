# 敏感密钥模板 —— 复制为 .env.secret（已 gitignore，绝不提交到公开仓库）
# .env.secret 只存在于真正运行容器的机器上（Mac mini），不随 git 同步。
# 详见根 CLAUDE.md「安全红线（公开仓库）」。

# Tailscale auth key（供 tailscale-gw 网关容器加入 tailnet）
# 在 https://login.tailscale.com/admin/settings/keys 生成：
#   - Reusable: 是（容器重建后仍可用）
#   - Ephemeral: 否（配合 state 卷持久化为稳定节点）
#   - 若 tailnet 启用了设备审批/标签 ACL：给该 key 打 tag（如 tag:patra-gw）并在 ACL 放行 tag → MacBook:6301
TS_AUTHKEY=tskey-auth-xxxxxxxxxxxx
