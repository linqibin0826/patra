<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Property for application name -->
    <property name="appName" value="patra-spring-boot-starter-object-storage"/>

    <!-- Console Appender: 测试专用配置,使用 %nopex 抑制堆栈跟踪 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <!-- %nopex: 不输出异常堆栈跟踪,只显示异常消息,避免测试输出被大量堆栈淹没 -->
            <pattern>%d{HH:mm:ss.SSS} %5p [${appName}] [%t] %-40.40logger{39} : %m%nopex%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- Root Logger: WARN level (减少测试输出) -->
    <root level="WARN">
        <appender-ref ref="CONSOLE"/>
    </root>

    <!-- Application Logger: INFO level (only for com.patra.* classes) -->
    <logger name="com.patra" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE"/>
    </logger>

</configuration>
