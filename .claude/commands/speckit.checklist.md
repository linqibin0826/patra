---
description: 基于用户需求为当前特性生成自定义检查清单。
---

## 检查清单目的："需求编写的单元测试"

**核心概念**:检查清单是**需求编写的单元测试** - 它们验证给定领域中需求的质量、清晰度和完整性。

**不用于验证/测试**:

- ❌ 不是"验证接口返回正确数据"
- ❌ 不是"测试数据库操作正常"
- ❌ 不是"确认 API 返回 200"
- ❌ 不是检查代码/实现是否符合规格说明

**用于需求质量验证**:

- ✅ "是否为所有聚合根定义了清晰的边界？"（完整性）
- ✅ "所有 Repository 接口定义是否一致？"（一致性）

**比喻**:如果你的规格说明是用中文编写的代码，那么检查清单就是它的单元测试套件。你测试的是需求是否编写良好、完整、明确且准备好实施 - 而不是实施是否有效。

## 用户输入

```text
$ARGUMENTS
```

你**必须**在继续之前考虑用户输入（如果不为空）。

## 执行步骤

1. **设置**:从仓库根目录运行 `.specify/scripts/bash/check-prerequisites.sh --json` 并解析 JSON 以获取 FEATURE_DIR 和 AVAILABLE_DOCS 列表。
   - 所有文件路径必须是绝对路径。
   - 对于包含单引号的参数，如 "I'm Groot"，使用转义语法：例如 'I'\''m Groot'（或者如果可能使用双引号："I'm Groot"）。

2. **澄清意图（动态）**:派生最多三个初始的上下文澄清问题（无预设目录）。它们必须：
   - 从用户的措辞 + 从 spec/plan/tasks 提取的信号生成
   - 仅询问会实质性改变检查清单内容的信息
   - 如果在 `$ARGUMENTS` 中已经明确则单独跳过
   - 偏好精确性而非广度

   生成算法：
   1. 提取信号:特性领域关键字（例如，catalog、data、api、架构）、风险指标（"关键"、"必须"、"合规"）、利益相关者提示（"集成测试"、"审查"、"数据一致性"）和明确的可交付成果（"SSOT"、"六边形架构"、"DDD"）。
   2. 将信号聚类到候选焦点区域（最多 4 个）并按相关性排名。
   3. 如果不明确则识别可能的受众和时机（规格编写者、架构评审、实施前）。
   4. 检测缺失的维度:范围广度、深度/严谨性、风险强调、排除边界、可衡量的验收标准。
   5. 从这些原型中选择制定问题：
      - 范围细化（例如，"这应该包括与外部 API 的集成接触点还是仅限于本地业务逻辑？"）
      - 风险优先级（例如，"这些潜在风险区域中哪些应该接受强制性门控检查？"）
      - 深度校准（例如，"这是轻量级的规格检查还是正式的架构评审？"）
      - 受众框架（例如，"这将仅由规格编写者使用还是在架构评审中使用？"）
      - 边界排除（例如，"我们应该明确排除本轮的性能调优需求吗？"）
      - 场景类别差距（例如，"未检测到异常处理需求 - 数据源失败路径是否在范围内？"）

   问题格式规则：
   - 如果呈现选项，生成包含以下列的紧凑表格:选项 | 候选 | 为什么重要
   - 最多限制 A–E 选项；如果自由形式答案更清晰则省略表格
   - 永远不要要求用户重述他们已经说过的话
   - 避免推测性类别（无幻觉）。如果不确定，明确询问："确认 X 是否属于范围。"

   交互不可能时的默认值：
   - 深度:标准
   - 受众:规格编写者 + 架构评审者
   - 焦点:前 2 个相关性聚类

   输出问题（标记 Q1/Q2/Q3）。回答后:如果 ≥2 个场景类别（异常/边界/非功能领域）仍不清楚，你可以提出最多两个更有针对性的后续问题（Q4/Q5），每个都有一行理由（例如，"未解决的数据一致性风险"）。不要超过五个总问题。如果用户明确拒绝更多则跳过升级。

3. **理解用户请求**:结合 `$ARGUMENTS` + 澄清答案：
   - 派生检查清单主题（例如，架构、领域模型、API、数据访问）
   - 整合用户提到的明确必备项目
   - 将焦点选择映射到类别脚手架
   - 从 spec/plan/tasks 推断任何缺失的上下文（不要幻觉）

4. **加载特性上下文**:从 FEATURE_DIR 读取：
   - spec.md:特性需求和范围
   - plan.md（如果存在）:技术细节、依赖
   - tasks.md（如果存在）:实现任务
   - `.specify/memory/constitution.md`:架构规范和 CHK-* 验证项（用于架构合规性、DDD、SSOT、测试策略检查清单）

   **上下文加载策略**：
   - 仅加载与活动焦点区域相关的必要部分（避免完整文件转储）
   - 偏好将长部分总结为简洁的场景/需求要点
   - 使用渐进披露:仅在检测到差距时添加后续检索
   - 如果源文档很大，生成中期摘要项目而不是嵌入原始文本

5. **生成检查清单** - 创建"需求的单元测试"：
   - 如果 `FEATURE_DIR/checklists/` 目录不存在则创建
   - 生成唯一的检查清单文件名：
     - 基于领域使用简短、描述性的名称（例如，`architecture.md`、`domain-model.md`、`api.md`、`data-access.md`）
     - 格式：`[domain].md`
     - 如果文件存在，则追加到现有文件
   - 从 CHK001 开始顺序编号项目
   - 每次 `/speckit.checklist` 运行都会创建一个新文件（永远不会覆盖现有检查清单）

   **核心原则 - 测试需求，而不是实现**：
   每个检查清单项目必须评估需求本身的：
   - **完整性**:是否存在所有必要的需求？
   - **清晰度**:需求是否明确且具体？
   - **一致性**:需求是否相互一致？
   - **可衡量性**:需求是否可以客观验证？
   - **覆盖范围**:是否涵盖所有场景/边界情况？

   **类别结构** - 按需求质量维度分组项目：
   - **需求完整性**（是否记录了所有必要的需求？）
   - **需求清晰度**（需求是否具体且明确？）
   - **需求一致性**（需求是否一致且无冲突？）
   - **验收标准质量**（成功标准是否可衡量？）
   - **场景覆盖**（是否涵盖所有流程/案例？）
   - **边界情况覆盖**（是否定义了边界条件？）
   - **非功能需求**（性能、安全、可扩展性等 - 是否已指定？）
   - **依赖和假设**（是否已记录并验证？）
   - **歧义和冲突**（需要澄清什么？）
   - **架构合规性需求质量**（是否明确定义了架构约束？参考 `.specify/memory/constitution.md` 的 CHK-* 验证项）

   **如何编写检查清单项目 - "中文的单元测试"**：

   ❌ **错误**（测试实现）：
   - "验证期刊列表 API 返回 JSON"
   - "测试分页功能正常工作"
   - "确认数据库查询返回正确数据"

   ✅ **正确**（测试需求质量）：
   - "是否明确指定了期刊列表的分页参数（页码、页大小）？"[完整性]
   - "是否用具体的响应时间量化了'高性能'？"[清晰度]
   - "所有查询接口的排序规则需求是否一致？"[一致性]
   - "是否为所有查询接口定义了空结果的处理需求？"[覆盖范围]
   - "是否指定了数据源超时时的降级策略？"[边界情况]
   - "是否定义了大数据量查询的性能需求？"[完整性]
   - "规格说明是否定义了聚合根的边界和不变量？"[清晰度]

   **项目结构**：
   每个项目应遵循此模式：
   - 询问需求质量的问题格式
   - 专注于规格说明/计划中已写（或未写）的内容
   - 在括号中包含质量维度 [完整性/清晰度/一致性等]
   - 检查现有需求时引用规格说明部分 `[Spec §X.Y]`
   - 检查缺失需求时使用 `[Gap]` 标记

   **按质量维度的示例**：

   完整性：
   - "是否为所有外部 API 调用定义了错误处理需求？[Gap]"
   - "是否为所有聚合根指定了业务不变量？[完整性]"
   - "是否定义了数据迁移的需求？[Gap]"

   清晰度：
   - "是否用具体的时间阈值量化了'实时同步'？[清晰度, Spec §NFR-2]"
   - "是否明确定义了'相关期刊'的选择标准？[清晰度, Spec §FR-5]"
   - "是否用可衡量的指标定义了'数据完整性'？[歧义, Spec §FR-4]"

   一致性：
   - "所有微服务的认证需求是否一致？[一致性, Spec §NFR-10]"
   - "Domain 层和 Infrastructure 层的 Repository 接口定义是否一致？[一致性]"

   覆盖范围：
   - "是否为零数据场景（无期刊）定义了需求？[覆盖范围, 边界情况]"
   - "是否涵盖了并发数据修改冲突的场景？[覆盖范围, Gap]"
   - "是否为外部 API 调用失败指定了需求？[覆盖范围, 异常流程]"

   可衡量性：
   - "性能需求是否可衡量/可测试？[验收标准, Spec §NFR-1]"
   - "'数据一致性'是否可以客观验证？[可衡量性, Spec §NFR-2]"

   **场景分类和覆盖**（需求质量焦点）：
   - 检查以下场景是否存在需求:主要、备用、异常/错误、恢复、非功能场景
   - 对于每个场景类别，询问："[场景类型]需求是否完整、清晰且一致？"
   - 如果缺少场景类别:"[场景类型]需求是有意排除还是缺失？[Gap]"
   - 当发生状态突变时包括弹性/回滚:"是否为数据迁移失败定义了回滚需求？[Gap]"

   **可追溯性需求**：
   - 最低要求:≥80% 的项目必须包含至少一个可追溯性引用
   - 每个项目应引用:规格说明部分 `[Spec §X.Y]`，或使用标记：`[Gap]`、`[Ambiguity]`、`[Conflict]`、`[Assumption]`
   - 如果不存在 ID 系统:"是否建立了需求和验收标准 ID 方案？[可追溯性]"

   **暴露和解决问题**（需求质量问题）：
   询问有关需求本身的问题：
   - 歧义:"术语'实时'是否用具体指标量化？[歧义, Spec §NFR-1]"
   - 冲突:"§FR-10 和 §FR-10a 之间的数据访问需求是否冲突？[冲突]"
   - 假设:"'始终可用的 PubMed API'假设是否已验证？[假设]"
   - 依赖:"是否记录了外部数据源的依赖需求？[依赖, Gap]"
   - 缺失定义:"是否用可衡量的标准定义了'聚合边界'？[Gap]"

   **内容整合**：
   - 软上限:如果原始候选项目 > 40，按风险/影响优先级排序
   - 合并检查相同需求方面的近似重复项
   - 如果 >5 个低影响边界情况，创建一个项目:"需求中是否涵盖了边界情况 X、Y、Z？[覆盖范围]"

   **🚫 绝对禁止** - 这些使其成为实现测试，而不是需求测试：
   - ❌ 任何以"验证"、"测试"、"确认"、"检查" + 实现行为开头的项目
   - ❌ 对代码执行、用户操作、系统行为的引用
   - ❌ "正确返回"、"正常工作"、"按预期运行"
   - ❌ "调用"、"查询"、"存储"、"加载"、"执行"
   - ❌ 测试用例、测试计划、QA 程序
   - ❌ 实现细节（框架、MyBatis、Spring）

   **✅ 必需模式** - 这些测试需求质量：
   - ✅ "是否为[场景]定义/指定/记录了[需求类型]？"
   - ✅ "是否用具体标准量化/澄清了[模糊术语]？"
   - ✅ "[部分 A]和[部分 B]之间的需求是否一致？"
   - ✅ "[需求]是否可以客观衡量/验证？"
   - ✅ "需求中是否涵盖了[边界情况/场景]？"
   - ✅ "规格说明是否定义了[缺失方面]？"

6. **结构参考**:按照 `.specify/templates/checklist-template.md` 中的规范模板生成检查清单，包括标题、元部分、类别标题和 ID 格式。如果模板不可用，使用:H1 标题、目的/创建的元行、包含 `- [ ] CHK### <需求项目>` 行的 `##` 类别部分，其中全局递增的 ID 从 CHK001 开始。

7. **报告**:输出创建的检查清单的完整路径、项目计数，并提醒用户每次运行都会创建一个新文件。总结：
   - 选择的焦点区域
   - 深度级别
   - 参与者/时机
   - 纳入的任何明确用户指定的必备项目

**重要**:每次 `/speckit.checklist` 命令调用都会使用简短、描述性的名称创建检查清单文件，除非文件已存在。这允许：

- 不同类型的多个检查清单（例如，`architecture.md`、`domain-model.md`、`api.md`）
- 简单、易记的文件名，指示检查清单目的
- 在 `checklists/` 文件夹中轻松识别和导航

为避免混乱，使用描述性类型并在完成后清理过时的检查清单。

## 检查清单类型示例和样本项目

**架构合规性需求质量（Patra 项目）**:`architecture.md`

样本项目（测试需求质量，参考 constitution.md 的 CHK-* 验证项）：

- "是否明确指定了六边形架构的层次边界？[完整性, Spec §Architecture]"
- "是否清晰定义了 Domain 层的依赖约束（仅 Lombok/Hutool/patra-common）？[清晰度, CHK-ARCH-001]"
- "是否一致定义了依赖方向（Adapter → App → Domain ← Infra）？[一致性, CHK-ARCH-002]"
- "是否为事务边界指定了明确的需求（@Transactional 仅在应用层）？[Gap, CHK-ARCH-003]"
- "是否定义了 DO（数据对象）的封装需求（DO 不离开 Infrastructure 层）？[覆盖范围, CHK-ARCH-004]"
- "架构约束需求是否可以通过 ArchUnit 测试验证？[可衡量性]"

**DDD 设计需求质量（Patra 项目）**:`domain-model.md`

样本项目（测试领域模型需求质量）：

- "是否明确识别了所有聚合根及其边界？[完整性, Spec §Domain Model, CHK-DDD-001]"
- "是否清晰定义了实体间的关系？[清晰度, CHK-DDD-002]"
- "是否指定了领域事件及其发布时机（使用过去时命名）？[覆盖范围, CHK-DDD-003]"
- "是否一致定义了值对象的不可变性需求（使用 record 或 final 类）？[一致性]"
- "是否为聚合根定义了业务不变量？[Gap]"
- "是否记录了 Port 接口的抽象策略（在 Domain 层定义，Infrastructure 层实现）？[CHK-DDD-004]"

**SSOT 原则需求质量（Patra 项目）**:`ssot-compliance.md`

样本项目（测试单一事实来源需求）：

- "是否明确要求从 patra-registry 获取 Provenance 配置？[完整性, CHK-SSOT-001]"
- "是否禁止硬编码数据源配置？[清晰度, Spec §Configuration]"
- "是否定义了数据字典的单一来源需求（从 patra-registry 动态加载）？[Gap, CHK-SSOT-002]"
- "是否指定了元数据和映射规则的版本化需求？[覆盖范围, CHK-SSOT-003]"
- "patra-registry 的配置更新需求是否支持运行时热更新？[边界情况]"

**API 需求质量**:`api.md`

样本项目：

- "是否为所有失败场景指定了错误响应格式？[完整性]"
- "是否用具体阈值量化了响应时间需求？[清晰度]"
- "所有端点的认证需求是否一致？[一致性]"
- "是否为外部依赖定义了重试/超时需求？[覆盖范围, Gap]"
- "是否在需求中记录了 API 版本控制策略？[Gap]"
- "是否定义了分页查询的标准格式（页码、页大小、总数）？[完整性]"

**数据访问需求质量**:`data-access.md`

样本项目：

- "是否明确定义了所有 Repository 接口的方法签名？[完整性]"
- "是否为数据库事务定义了隔离级别需求？[清晰度]"
- "是否为大数据量查询定义了性能需求（响应时间、内存限制）？[覆盖范围, Gap]"
- "是否定义了数据库连接池的配置需求？[Gap]"
- "是否为乐观锁冲突定义了处理需求？[边界情况, Gap]"

**测试策略需求质量（Patra 项目）**:`test-strategy.md`

样本项目（测试测试策略需求，参考 patra-tdd-development）：

- "是否明确要求 TDD 方法（测试先于代码）？[完整性, Spec §Testing]"
- "是否定义了 Red-Green-Refactor 循环的执行要求？[清晰度, TDD 工作流]"
- "是否定义了 IT/E2E 测试的模块位置需求（必须在 patra-{service}-boot 模块）？[一致性, 测试位置规范]"
- "是否指定了 TestContainers 用于集成测试的需求？[覆盖范围, TDD 最佳实践]"
- "是否要求 ArchUnit 架构测试以验证依赖方向？[Gap, 架构合规性]"
- "是否为 Domain 层单元测试定义了无框架依赖的需求？[清晰度, 六边形架构原则]"
- "是否为各层定义了测试覆盖率要求（Domain ≥80%, Application ≥70%）？[可衡量性, CHK-TEST-001/002]"

**性能需求质量**:`performance.md`

样本项目：

- "是否用具体指标量化了性能需求（响应时间、吞吐量）？[清晰度]"
- "是否为所有关键业务流程定义了性能目标？[覆盖范围]"
- "是否指定了不同负载条件下的性能需求（正常/高峰/极限）？[完整性]"
- "性能需求是否可以客观衡量（有明确的基准和工具）？[可衡量性]"
- "是否为高负载场景定义了降级需求？[边界情况, Gap]"
- "是否定义了数据库查询的性能优化需求（索引、分页）？[Gap]"

**数据一致性需求质量**:`data-consistency.md`

样本项目：

- "是否定义了分布式事务的一致性保证需求（最终一致性 vs 强一致性）？[完整性]"
- "是否为跨聚合的数据变更定义了领域事件机制？[清晰度, CHK-DDD-003]"
- "是否定义了数据同步失败时的补偿机制？[覆盖范围, Gap]"
- "是否为并发修改定义了冲突解决策略（乐观锁、悲观锁）？[边界情况]"
- "是否定义了数据迁移的一致性验证需求？[Gap]"

## 反例：不应该做什么

**❌ 错误 - 这些测试实现，而不是需求：**

```markdown
- [ ] CHK001 - 验证期刊列表 API 返回 JSON 格式 [Spec §FR-001]
- [ ] CHK002 - 测试分页功能正常工作 [Spec §FR-003]
- [ ] CHK003 - 确认数据库查询返回正确数据 [Spec §FR-010]
- [ ] CHK004 - 检查聚合根状态转换正确 [Spec §FR-005]
```

**✅ 正确 - 这些测试需求质量：**

```markdown
- [ ] CHK001 - 是否明确指定了期刊列表的响应格式（JSON Schema）？[完整性, Spec §FR-001]
- [ ] CHK002 - 是否为所有查询接口一致定义了分页参数（页码、页大小）？[一致性, Spec §FR-003]
- [ ] CHK003 - 所有 Repository 接口的查询方法签名是否清晰定义？[清晰度, Spec §FR-010]
- [ ] CHK004 - 是否记录了聚合根的状态转换规则和业务不变量？[Gap, Spec §FR-005]
- [ ] CHK005 - 是否为数据库查询失败定义了异常处理需求？[Gap]
- [ ] CHK006 - "高性能"需求是否可以客观衡量（有明确的响应时间阈值）？[可衡量性, Spec §NFR-001]
```

**关键区别：**

- 错误:测试系统是否正常工作
- 正确:测试需求是否正确编写
- 错误:行为验证
- 正确:需求质量验证
- 错误:"它是否执行 X？"
- 正确:"X 是否明确指定？"
