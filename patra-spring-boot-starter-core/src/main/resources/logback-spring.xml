<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Include Spring Boot's logback extensions for %clr and %wEx support -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <!-- Property for application name (fallback to "application") -->
    <springProperty scope="context" name="appName" source="spring.application.name" defaultValue="application"/>

    <!-- Property for log file path (centralized in project root) -->
    <!-- Default to ${user.dir}/logs if logging.file.path not set -->
    <springProperty scope="context" name="LOG_PATH" source="logging.file.path" defaultValue="logs"/>
    <property name="LOG_FILE" value="${LOG_PATH}/${appName}"/>

    <!-- Register custom OTel converters for trace context -->
    <!-- Note: Logback 1.5+ uses 'class' instead of deprecated 'converterClass' -->
    <conversionRule conversionWord="traceId"
                    class="com.patra.starter.core.logging.TraceIdConverter"/>
    <conversionRule conversionWord="spanId"
                    class="com.patra.starter.core.logging.SpanIdConverter"/>

    <!-- Console Appender with colors and trace context -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr(%5p) %clr([${appName}]){blue} %clr([%traceId/%spanId]){magenta} %clr([%t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n%wEx</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- File Appender with trace context -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}.log</file>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %5p [${appName}] [%traceId/%spanId] [%t] %-40.40logger{39} : %m%n</pattern>
            <charset>UTF-8</charset>
        </encoder>

        <!-- Rolling Policy -->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- Daily rollover with max file size -->
            <fileNamePattern>${LOG_FILE}.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <!-- Each file max size -->
            <maxFileSize>100MB</maxFileSize>
            <!-- Total size of all archive files, if exceeded, oldest files deleted -->
            <totalSizeCap>10GB</totalSizeCap>
            <!-- Keep logs for 30 days -->
            <maxHistory>30</maxHistory>
        </rollingPolicy>

        <!-- Filter: only INFO and above -->
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>INFO</level>
        </filter>
    </appender>

    <!-- Root Logger: INFO level (default for all libraries) -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

    <!-- Application Logger: DEBUG level (only for com.patra.* classes) -->
    <logger name="com.patra" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </logger>

</configuration>
