package com.patra.ingest.app.config;

import org.springframework.context.annotation.Configuration;

/**
 * 数据采集应用层配置
 *
 * @author linqibin
 * @since 0.1.0
 */
@Configuration
public class IngestAppConfig {
    
    // 配置类，Spring会自动扫描并注册标记了@Service的应用服务
    
}
