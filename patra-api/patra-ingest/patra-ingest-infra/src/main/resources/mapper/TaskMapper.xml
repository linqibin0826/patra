<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.patra.ingest.infra.persistence.mapper.TaskMapper">

    <!--
        CAS 抢占租约（步骤 0）
        
        策略：仅针对 QUEUED 状态且满足以下条件的任务进行 CAS 更新：
        1. status_code='QUEUED' AND idempotent_key=#{idem}
        2. scheduled_at IS NULL OR scheduled_at <= #{now}（尊重 notBefore）
        3. leased_until IS NULL OR leased_until <= #{now} OR lease_owner=#{owner}（可接管）
        
        更新字段：lease_owner、leased_until、lease_count+1
        
        返回：受影响行数（1=成功，0=失败）
    -->
    <update id="tryAcquireLease">
        UPDATE ing_task
           SET lease_owner = #{owner},
               leased_until = DATE_ADD(#{now}, INTERVAL #{ttlSec} SECOND),
               lease_count = lease_count + 1
         WHERE id = #{taskId}
           AND status_code = 'QUEUED'
           AND idempotent_key = #{idem}
           AND (scheduled_at IS NULL OR scheduled_at &lt;= #{now})
           AND (leased_until IS NULL OR leased_until &lt;= #{now} OR lease_owner = #{owner})
    </update>

    <!--
        置任务为 RUNNING 状态并更新租约（步骤 1）
        
        前置条件：WHERE lease_owner=#{owner}（防止被窃取）
        更新字段：status_code='RUNNING'、started_at、last_heartbeat_at、leased_until
        
        返回：受影响行数（1=成功，0=租约丢失）
    -->
    <update id="markRunningWithLease">
        UPDATE ing_task
           SET status_code = 'RUNNING',
               started_at = #{now},
               last_heartbeat_at = #{now},
               leased_until = DATE_ADD(#{now}, INTERVAL #{ttlSec} SECOND)
         WHERE id = #{taskId}
           AND lease_owner = #{owner}
    </update>

    <!--
        心跳续租
        
        前置条件：WHERE lease_owner=#{owner}
        更新字段：leased_until、last_heartbeat_at、lease_count+1
        
        返回：受影响行数（1=成功，0=租约丢失）
    -->
    <update id="renewLease">
        UPDATE ing_task
           SET leased_until = DATE_ADD(#{now}, INTERVAL #{ttlSec} SECOND),
               last_heartbeat_at = #{now},
               lease_count = lease_count + 1
         WHERE id = #{taskId}
           AND lease_owner = #{owner}
    </update>

</mapper>
