# Optional: timeouts and log level
feign:
  client:
    config:
      default:
        connectTimeout: 2000
        readTimeout: 5000


xxl:
  job:
    admin:
      addresses: http://papertrace.com:7070/xxl-job-admin   # Admin externally accessible URL
    executor:
      ip: host.docker.internal
      logpath: ${PAPERTRACE_LOG_DIR:/Users/linqibin/IdeaProjects/medoc/Papertrace-api/logs}/patra-ingest/xxl-job
      logretentiondays: 30

patra:
  ingest:
    outbox-relay:
      enabled: true
      batch-size: 200
      lease-duration: PT30S

