<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.patra.registry.infra.persistence.mapper.dictionary.RegSysDictTypeMapper">

  <resultMap id="RegSysDictTypeResultMap" type="com.patra.registry.infra.persistence.entity.dictionary.RegSysDictTypeDO">
    <id column="id" property="id"/>
  </resultMap>

  <select id="selectByTypeCode" resultMap="RegSysDictTypeResultMap">
    SELECT * FROM sys_dict_type WHERE type_code = #{typeCode} AND deleted = 0
  </select>

  <select id="selectAllEnabled" resultMap="RegSysDictTypeResultMap">
    SELECT * FROM sys_dict_type WHERE deleted = 0 ORDER BY type_code
  </select>

  <select id="countTotal" resultType="int">
    SELECT COUNT(*) FROM sys_dict_type WHERE deleted = 0
  </select>

  <select id="selectCustomizableTypes" resultMap="RegSysDictTypeResultMap">
    SELECT * FROM sys_dict_type WHERE allow_custom_items = 1 AND deleted = 0 ORDER BY type_code
  </select>

  <select id="selectSystemTypes" resultMap="RegSysDictTypeResultMap">
    SELECT * FROM sys_dict_type WHERE is_system = 1 AND deleted = 0 ORDER BY type_code
  </select>

</mapper>
