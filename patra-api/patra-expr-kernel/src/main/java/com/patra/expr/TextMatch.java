package com.patra.expr;

/// TERM 操作的文本匹配语义。
///
/// 定义了文本值应如何与搜索查询中的字段内容匹配。
public enum TextMatch {
  /// 短语匹配。
  ///
  /// 将文本作为完整短语匹配，保留字序。
  PHRASE,

  /// 精确匹配。
  ///
  /// 精确匹配文本，不进行分词。
  EXACT,

  /// 任意令牌匹配。
  ///
  /// 如果文本中的任何令牌在字段中找到，则匹配。
  ANY
}
