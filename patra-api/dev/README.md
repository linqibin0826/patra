# 开发任务管理

Patra 开发的任务管理目录。

## 结构

```
dev/
└── active/           # 活跃的开发任务
    └── [task-name]/
        ├── [task-name]-plan.md      # 综合计划
        ├── [task-name]-context.md   # 关键文件、决策、依赖
        └── [task-name]-tasks.md     # 任务清单
```

## 使用方法

使用 `/dev-docs` 命令创建结构化任务计划。
