# 架构评审检查清单: MeSH 数据首次导入

**特性**: MeSH 数据首次导入
**检查清单类型**: 架构评审（严格级别）
**创建日期**: 2025-11-20
**目的**: 在进入实施阶段前，验证规格说明的需求质量、DDD 设计、架构合规性、性能和可观察性需求是否完整、清晰、一致且可衡量。

**说明**: 本检查清单是"需求的单元测试"，用于验证规格说明本身的质量，而非实现的正确性。每个验证项检查需求是否已明确定义、是否一致、是否可衡量。

---

## I. 需求完整性（功能需求）

### FR - 核心导入流程需求

- [ ] **CHK001** - 是否为 MeSH XML 文件的下载来源（NLM 官方 URL）定义了明确的需求？[完整性, Spec §FR-001]
- [ ] **CHK002** - 是否为 XML 文件的解析策略（流式解析 vs 全量加载）定义了明确的需求？[Gap]
- [ ] **CHK003** - 是否为 6 张表的串行导入顺序定义了明确的依赖关系需求？[完整性, Spec §FR-003, 澄清会话]
- [ ] **CHK004** - 是否为批次大小（1000-2000 条）的动态调整策略定义了需求？[清晰度, Spec §边界情况]
- [ ] **CHK005** - 是否为每批次的独立事务提交定义了明确的需求？[完整性, Spec §FR-004]
- [ ] **CHK006** - 是否为断点续传的检查点保存时机（批次成功后）定义了需求？[完整性, Spec §FR-007]

### FR - 任务状态管理需求

- [ ] **CHK007** - 是否为任务状态机的所有合法转换定义了明确的需求（待处理 → 处理中 → 成功/失败）？[完整性, Spec §FR-005]
- [ ] **CHK008** - 是否为非法状态转换（如"成功" → "处理中"）的防御需求定义了规则？[Gap, Spec §NFR 架构约束 DDD 不变量]
- [ ] **CHK009** - 是否为并发导入请求的互斥锁定需求定义了明确的规则？[完整性, Spec §边界情况]
- [ ] **CHK010** - 是否为任务状态变更的持久化时机（同步 vs 异步）定义了需求？[Gap]

### FR - 异常处理和重试需求

- [ ] **CHK011** - 是否为批次失败后的重试策略（重试次数、重试间隔、指数退避）定义了明确的需求？[Gap, Spec §FR-012 仅提到"记录重试次数"]
- [ ] **CHK012** - 是否为 NLM 数据源不可用时的降级策略定义了需求？[完整性, Spec §边界情况]
- [ ] **CHK013** - 是否为 XML 格式异常记录的跳过策略（跳过 vs 中止）定义了需求？[完整性, Spec §边界情况]
- [ ] **CHK014** - 是否为数据库连接中断时的幂等性保证（避免重复插入）定义了需求？[完整性, Spec §边界情况]
- [ ] **CHK015** - 是否为磁盘空间不足时的错误处理和清理策略定义了需求？[完整性, Spec §边界情况]

### FR - 管理接口需求

- [ ] **CHK016** - 是否为所有管理接口（开始导入、重试失败、清除进度、查询进度）定义了完整的 REST API 规范（HTTP 方法、路径、请求/响应格式）？[Gap, Spec §FR-008~011 仅提到"管理接口"]
- [ ] **CHK017** - 是否为管理接口的幂等性保证（如重复调用"开始导入"）定义了需求？[Gap, Spec §边界情况提到"拒绝新请求"]
- [ ] **CHK018** - 是否为管理接口的认证和授权需求定义了明确的规则？[完整性, Spec §NFR-004]
- [ ] **CHK019** - 是否为查询进度接口的响应时间需求定义了具体的阈值（≤ 1 秒）？[完整性, Spec §SC-003]

---

## II. 需求完整性（非功能需求）

### NFR - 性能需求

- [ ] **CHK020** - 是否为全量导入的总耗时目标（60 分钟）定义了可衡量的性能需求？[完整性, Spec §NFR-001]
- [ ] **CHK021** - 是否为平均处理速度（≥ 100 条/秒）在不同数据量表（限定词 100 vs 入口术语 25 万）的适用性定义了需求？[清晰度, Spec §NFR-001]
- [ ] **CHK022** - 是否为单批次提交时间的 P95（≤ 5 秒）定义了明确的性能需求？[完整性, Spec §NFR-002]
- [ ] **CHK023** - 是否为内存占用峰值（≤ 2GB）在 XML 解析和批量插入时的分配策略定义了需求？[清晰度, Spec §NFR-003]
- [ ] **CHK024** - 是否为 CPU 使用率 P95（≤ 70%）定义了需求？[完整性, Spec §SC-006]

### NFR - 可观察性需求

- [ ] **CHK025** - 是否为每个批次处理的日志级别（INFO）和内容（批次编号、处理记录数、耗时、状态）定义了明确的需求？[完整性, Spec §NFR-006]
- [ ] **CHK026** - 是否为关键操作（任务启动、表切换、任务完成/失败）的日志内容（任务 ID、表名、时间戳）定义了需求？[完整性, Spec §NFR-007]
- [ ] **CHK027** - 是否为 SkyWalking 追踪的 Span 粒度（批次级别 vs 表级别）定义了需求？[清晰度, Spec §NFR-008]
- [ ] **CHK028** - 是否为 Micrometer 监控指标的具体名称（任务级别、表级别、批次级别）定义了需求？[Gap, Spec §NFR-009 仅提到"包含统计"]
- [ ] **CHK029** - 是否为失败批次的错误日志格式（XML 行号、记录内容摘要）定义了明确的需求？[完整性, Spec §NFR-010]

### NFR - 安全需求

- [ ] **CHK030** - 是否为管理接口的认证机制（JWT vs Session vs API Key）定义了明确的需求？[Gap, Spec §NFR-004 仅提到"验证管理员权限"]
- [ ] **CHK031** - 是否为管理员权限的细粒度划分（查询 vs 修改）定义了需求？[Gap]
- [ ] **CHK032** - 是否为错误日志的敏感信息脱敏规则（数据库连接字符串、密钥）定义了需求？[完整性, Spec §NFR-005]

---

## III. 需求清晰度和可衡量性

### 清晰度 - 模糊术语量化

- [ ] **CHK033** - "实时监控"的含义是否用具体的刷新频率量化（如每 5 秒更新进度）？[歧义, Spec §用户故事 2]
- [ ] **CHK034** - "预计剩余时间"的计算方式是否明确定义（基于平均速度 vs 最近 N 批次速度）？[歧义, Spec §FR-011]
- [ ] **CHK035** - "数据完整性"的验证标准是否可衡量（与 NLM 官方数据对比差异 ≤ 0.5%）？[可衡量性, Spec §SC-004]
- [ ] **CHK036** - "失败率 ≤ 5%"的计算基准是否明确（批次失败数 / 总批次数）？[可衡量性, Spec §SC-005]
- [ ] **CHK037** - "数据质量问题"的定义是否具体（格式异常 vs 数据缺失 vs 数量不符）？[歧义, Spec §SC-007]

### 可衡量性 - 验收标准

- [ ] **CHK038** - 所有成功标准（SC-001 ~ SC-008）是否可以通过自动化测试客观验证？[可衡量性, Spec §成功标准]
- [ ] **CHK039** - 是否为每个性能需求（NFR-001 ~ NFR-003）定义了性能测试的基准数据和测试环境？[Gap]
- [ ] **CHK040** - 是否为"断点续传"的验证标准定义了具体的测试场景（中断时机、恢复正确性）？[可衡量性, Spec §SC-002]

---

## IV. 需求一致性

### 内部一致性

- [ ] **CHK041** - FR-003（串行导入顺序）与边界情况（动态调整批次大小）的需求是否一致？[一致性]
- [ ] **CHK042** - FR-005（任务状态）与 FR-009（重试失败任务）的状态转换需求是否一致？[一致性]
- [ ] **CHK043** - NFR-001（60 分钟完成）与 NFR-002（单批次 P95 ≤ 5 秒）和批次数量（~350 批次）的需求是否一致（350 批次 × 5 秒 = ~30 分钟，符合 60 分钟目标）？[一致性]
- [ ] **CHK044** - SC-002（1 分钟内恢复）与 FR-009（重试失败任务）的启动时间需求是否一致？[一致性]

### 与 Patra 项目规范一致性

- [ ] **CHK045** - 配置管理需求（使用 Nacos）是否与 Patra 项目的 SSOT 原则（patra-registry）一致？[一致性, Spec §NFR 配置管理说明, 澄清会话]
- [ ] **CHK046** - 监控指标需求（Micrometer + Actuator）是否与 Patra 项目的可观察性技术栈一致？[一致性, Spec §NFR-009, 澄清会话]
- [ ] **CHK047** - 聚合根命名（MeshImportAggregate）是否遵循 Patra 项目的命名约定（领域概念 + Aggregate 后缀）？[一致性, Spec §NFR 架构约束 DDD, 澄清会话]

---

## V. DDD 设计质量

### 聚合边界和不变量

- [ ] **CHK048** - 是否明确定义了 MeshImportAggregate 的聚合边界（包含任务状态、总体进度、失败批次摘要）？[完整性, Spec §澄清会话, Spec §NFR 架构约束 DDD]
- [ ] **CHK049** - 是否明确定义了 MeshImportAggregate 的业务不变量（任务状态转换的合法性、导入顺序的一致性）？[完整性, Spec §NFR 架构约束 DDD]
- [ ] **CHK050** - 是否明确定义了 TableProgress 值对象的结构（表名、已处理数/总数、当前状态）及其不可变性？[完整性, Spec §澄清会话]
- [ ] **CHK051** - 是否明确排除了 BatchDetail 在聚合根之外（存储在 Infrastructure 层）？[完整性, Spec §澄清会话]

### 强类型 ID 设计

- [ ] **CHK052** - 是否为所有实体定义了强类型 ID 需求（MeshImportId、DescriptorId、QualifierId、TreeNumberId、EntryTermId、ConceptId）？[完整性, Spec §澄清会话, Spec §NFR 架构约束 DDD]
- [ ] **CHK053** - 是否明确定义了强类型 ID 的实现方式（record vs final class）？[Gap]
- [ ] **CHK054** - 是否定义了强类型 ID 的序列化和持久化需求（如 MeshImportId 如何存储到数据库）？[Gap]

### 领域事件设计

- [ ] **CHK055** - 是否明确定义了所有关键状态变更的领域事件（MeshImportCompleted、MeshImportFailed）？[完整性, Spec §澄清会话, Spec §NFR 架构约束 DDD]
- [ ] **CHK056** - 是否明确定义了领域事件的有效负载（MeshImportId、导入统计、耗时、失败原因）？[完整性, Spec §NFR 架构约束 DDD]
- [ ] **CHK057** - 是否明确排除了批次级别和表级别的领域事件（仅发布任务级别事件）？[清晰度, Spec §澄清会话]
- [ ] **CHK058** - 是否定义了领域事件的发布时机（任务完成时 vs 状态变更后立即发布）？[Gap]
- [ ] **CHK059** - 是否定义了领域事件的命名规范（使用过去时：Completed、Failed）？[清晰度, 参考 Patra 项目 DDD 规范]

### 实体关系和 Port 接口

- [ ] **CHK060** - 是否明确定义了 6 张表对应的领域实体（Descriptor、Qualifier、TreeNumber、EntryTerm、Concept、PublicationMesh）及其关系？[Gap, Spec §FR-002 仅提到"提取数据"]
- [ ] **CHK061** - 是否定义了 MeshImportRepository Port 接口的方法签名（保存聚合根、查询任务状态、更新进度）？[Gap]
- [ ] **CHK062** - 是否定义了 MeshImportRepository 在 Domain 层定义、Infrastructure 层实现的需求？[完整性, 参考 CHK-DDD-004]

---

## VI. 架构合规性（六边形架构）

### 层次边界和依赖方向

- [ ] **CHK063** - 是否明确定义了 Domain 层的纯净性需求（仅允许 Lombok、Hutool、patra-common、Jackson）？[完整性, Spec §NFR 架构约束, 参考 CHK-ARCH-001]
- [ ] **CHK064** - 是否明确定义了依赖方向需求（Adapter → App → Domain ← Infra）？[完整性, Spec §NFR 架构约束, 参考 CHK-ARCH-002]
- [ ] **CHK065** - 是否禁止 Domain 层依赖 Spring 框架（org.springframework.*）？[完整性, Spec §NFR 架构约束, 参考 DomainLayerRules]
- [ ] **CHK066** - 是否禁止 App 层直接依赖 Infra 层的实现类？[完整性, 参考 CHK-ARCH-002, LayerDependencyRules]
- [ ] **CHK067** - 是否禁止循环依赖（包级别）？[完整性, 参考 CHK-ARCH-005, LayerDependencyRules]

### DO 封装和事务边界

- [ ] **CHK068** - 是否明确定义了 DO（Data Object）不离开 Infrastructure 层的封装需求？[完整性, 参考 CHK-ARCH-004, EncapsulationRules]
- [ ] **CHK069** - 是否明确定义了 @Transactional 仅在 Application 层（Orchestrator）的需求？[完整性, Spec §NFR 架构约束, 参考 CHK-ARCH-003]
- [ ] **CHK070** - 是否禁止 Domain 层使用 @Transactional 注解？[完整性, 参考 TransactionBoundaryRules]

### 命名约定和可测试性

- [ ] **CHK071** - 是否明确定义了 Port 接口在 domain.port 包的命名约定？[完整性, 参考 NamingConventionRules]
- [ ] **CHK072** - 是否明确定义了 Repository 实现在 infra 层的命名约定？[完整性, 参考 NamingConventionRules]
- [ ] **CHK073** - 是否明确定义了 Aggregate 在 domain.model.aggregate 包的命名约定？[完整性, Spec §澄清会话, 参考 NamingConventionRules]
- [ ] **CHK074** - 是否明确定义了 Orchestrator 在 app.usecase 包的命名约定？[Gap, 参考 NamingConventionRules]
- [ ] **CHK075** - 是否定义了架构合规性的自动化验证需求（使用 ArchUnit）？[Gap, 参考 IngestArchitectureTest]

---

## VII. 测试策略需求质量

### TDD 开发需求

- [ ] **CHK076** - 是否明确要求使用 TDD 开发方法（测试先于代码）？[Gap, 参考 patra-tdd-development]
- [ ] **CHK077** - 是否定义了 Red-Green-Refactor 循环的执行需求？[Gap, 参考 patra-tdd-development]

### 测试覆盖率需求

- [ ] **CHK078** - 是否为 Domain 层单元测试定义了覆盖率需求（≥ 80%）？[Gap, 参考 CHK-TEST-001]
- [ ] **CHK079** - 是否为 Application 层单元测试定义了覆盖率需求（≥ 70%）？[Gap, 参考 CHK-TEST-002]
- [ ] **CHK080** - 是否为 Infrastructure 层定义了单元测试和集成测试的需求？[Gap, 参考 CHK-TEST-003]
- [ ] **CHK081** - 是否为 Adapter 层定义了单元测试和切片测试的需求？[Gap, 参考 CHK-TEST-004]

### 测试位置和独立性需求

- [ ] **CHK082** - 是否明确定义了 IT/E2E 测试必须在 patra-catalog-boot 模块的需求？[Gap, 参考 CHK-TEST-005, TestingRules]
- [ ] **CHK083** - 是否定义了使用 TestContainers 进行集成测试的需求？[Gap, 参考 patra-tdd-development]
- [ ] **CHK084** - 是否禁止 Domain 层测试依赖 Spring 框架？[Gap, 参考 TestingRules]
- [ ] **CHK085** - 是否定义了测试类不能依赖其他测试类的需求（测试独立性）？[Gap, 参考 TestingRules]

---

## VIII. 配置管理需求质量

### Nacos 配置管理

- [ ] **CHK086** - 是否明确定义了所有导入任务配置通过 Nacos 管理的需求？[完整性, Spec §NFR 配置管理说明]
- [ ] **CHK087** - 是否明确列出了需要在 Nacos 中配置的具体项目（NLM 数据源 URL、批次大小、超时时间、重试策略）？[完整性, Spec §NFR 配置管理说明]
- [ ] **CHK088** - 是否定义了 Nacos 配置的命名空间和 Data ID 命名规范？[Gap]
- [ ] **CHK089** - 是否定义了 Nacos 配置的热更新需求（运行时生效 vs 重启生效）？[Gap]

### SSOT 排除说明

- [ ] **CHK090** - 是否明确说明了不引入 patra-registry 的 Provenance 配置的原因（简化架构依赖）？[完整性, Spec §NFR 配置管理说明]
- [ ] **CHK091** - 是否明确说明了 MeSH 数据本身就是数据字典，不从 patra-registry 获取的原因？[完整性, Spec §NFR 配置管理说明]
- [ ] **CHK092** - 是否与 Patra 项目的 SSOT 原则（CHK-SSOT-001 ~ 003）存在冲突？如有冲突，是否在 spec.md 的架构约束部分明确说明了例外情况的合理性？[一致性, 参考 constitution.md]

---

## IX. 场景覆盖和可追溯性

### 主要流程场景

- [ ] **CHK093** - 是否为首次导入（数据库为空）的完整流程定义了需求？[完整性, Spec §用户故事 1 验收场景 1]
- [ ] **CHK094** - 是否为断点续传（中断后恢复）的完整流程定义了需求？[完整性, Spec §用户故事 1 验收场景 2-3]
- [ ] **CHK095** - 是否为导入完成后的数据验证流程定义了需求？[完整性, Spec §FR-014]

### 异常和边界场景

- [ ] **CHK096** - 是否为所有边界情况（NLM 数据源不可用、XML 格式异常、数据库连接中断、磁盘空间不足、并发导入请求、数据量与预期不符）定义了需求？[完整性, Spec §边界情况]
- [ ] **CHK097** - 是否为批次级别的异常处理（重试 3 次失败后继续下一批次）定义了需求？[完整性, Spec §用户故事 1 验收场景 5]
- [ ] **CHK098** - 是否为任务级别的失败恢复（重试失败任务）定义了需求？[完整性, Spec §用户故事 3 验收场景 1]

### 可追溯性

- [ ] **CHK099** - 所有功能需求（FR-001 ~ FR-015）是否都有对应的验收场景或成功标准？[可追溯性]
- [ ] **CHK100** - 所有非功能需求（NFR-001 ~ NFR-010）是否都有对应的成功标准（SC-001 ~ SC-008）？[可追溯性]
- [ ] **CHK101** - 所有边界情况是否都有对应的功能需求或用户故事验收场景？[可追溯性]

---

## X. 缺失和歧义识别

### 关键缺失

- [ ] **CHK102** - 是否定义了 MeSH 数据的版本管理需求（如何识别当前导入的 MeSH 版本，如何支持版本升级）？[Gap]
- [ ] **CHK103** - 是否定义了导入任务的并发控制机制（分布式锁 vs 数据库唯一约束）？[Gap]
- [ ] **CHK104** - 是否定义了导入任务的审计日志需求（谁在何时触发了导入/重试/清除操作）？[Gap]
- [ ] **CHK105** - 是否定义了数据导入完成后的后续触发流程（如索引构建、缓存预热）？[Gap, Spec §NFR 架构约束 DDD 提到"触发后续流程"]

### 模糊和冲突

- [ ] **CHK106** - "清除进度重新开始"的需求是否与"断点续传"的需求存在冲突（如何确保清除操作不影响正在运行的任务）？[冲突, Spec §FR-010]
- [ ] **CHK107** - "批次失败后继续处理下一批次"的需求是否与"失败率 ≤ 5%"的成功标准一致（如果失败批次过多，任务是否应该中止）？[歧义, Spec §用户故事 1 验收场景 5, Spec §SC-005]
- [ ] **CHK108** - "数据一致性由业务层保证"的需求是否明确定义了关联关系验证的时机（导入时验证 vs 导入后验证）？[歧义, Spec §FR-013]

---

## 检查清单总结

**总验证项**: 108 项
**焦点领域**:
- ✅ 需求完整性和场景覆盖 (CHK001-CHK019, CHK093-CHK101)
- ✅ DDD 和聚合设计质量 (CHK048-CHK062)
- ✅ 架构合规性 (CHK063-CHK075)
- ✅ 性能和可观察性 (CHK020-CHK029)
- ✅ 配置管理 (CHK086-CHK092)
- ✅ 测试策略 (CHK076-CHK085)
- ✅ 需求清晰度和可衡量性 (CHK033-CHK040)
- ✅ 需求一致性 (CHK041-CHK047)
- ✅ 缺失和歧义识别 (CHK102-CHK108)

**使用时机**: 架构评审会议
**通过标准**: ≥ 90% 的验证项标记为 ✅（约 97 项），关键验证项（CHK-ARCH-*, CHK-DDD-*）100% 通过

**下一步**: 完成检查清单后，执行 `/speckit.plan` 进入实施计划阶段。
