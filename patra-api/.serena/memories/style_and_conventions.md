Coding standards: Java 21 with Maven, UTF-8, 4-space indentation, 120-char lines. Modules follow hexagonal/DDD layout with packages under com.patra.*, groupId com.papertrace. Domain layer is framework-free Java; adapters handle controllers/listeners; infra handles persistence and mappings. Use records for immutable value objects, Lombok for POJOs, MapStruct for mapping. Logging through SLF4J with parameterized messages; include trace IDs and business identifiers. Comments should explain intent; refactors must keep behavior unchanged. Naming patterns include *Orchestrator for app layer, *Command for commands, *Port interfaces, *Impl implementations.