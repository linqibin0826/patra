# Obsidian 语法与元数据规范

## 链接语法

| 类型 | 语法 |
|------|------|
| 内部文档 | `[[文件名]]` |
| 外部链接 | `[文本](URL)` |
| 嵌入文件 | `![[文件名]]` |
| 嵌入标题 | `![[文件名#标题]]` |
| 嵌入图片 | `![[image.png]]` / `![[image.png\|300]]` |

## 任务列表

| 状态 | 语法 | 状态 | 语法 |
|------|------|------|------|
| 未完成 | `- [ ] 任务` | 推迟 | `- [>] 任务` |
| 已完成 | `- [x] 任务` | 待确认 | `- [?] 任务` |
| 重要 | `- [!] 任务` | 取消 | `- [-] 任务` |

**禁止使用 emoji**（`✅ ⬜ 🔲`）

## Callouts 提示框

```markdown
> [!note] 标题（可选）
> 内容
```

常用类型：`[!note]` `[!tip]` `[!important]` `[!warning]` `[!danger]` `[!example]` `[!info]`

折叠语法：`[!note]+`（默认展开）或 `[!note]-`（默认折叠）

## 元数据规范（YAML Frontmatter）

**强制 `snake_case`** 键名，禁止 camelCase、PascalCase、空格

### 基础 Schema

```yaml
---
title: 文档标题
date: 2025-11-28
tags: [topic/subtopic]
aliases: [别名1, 别名2]
---
```

### 文档类型 Schema

| 类型 | 必填字段 | 状态流转 |
|------|----------|----------|
| ADR | `type: adr`, `adr_id`, `status`, `date_decided` | proposed → accepted / rejected / deprecated / superseded |
| Bug | `type: bug`, `status`, `severity`, `date` | open → resolved / wontfix |
| TIL | `type: til`, `date`, `topics` | — |
| Devlog | `type: devlog`, `date`, `commits`, `modules` | — |
| Design | `type: design`, `status`, `module` | draft → completed |

## 分类策略

| 机制 | 作用 | 最佳实践 |
|------|------|----------|
| 文件夹 | 物理边界（排他性） | 领域划分（devlog/bugs/learning） |
| 标签 | 横切关注点（非排他性） | 嵌套标签 `#topic/subtopic` |
| 链接 | 知识关联 | 仅链接强相关实体 |

**禁止**：用文件夹管理状态（用 Frontmatter `status` 字段）、过度链接

## MOC 策略

每个领域文件夹必须有 `_MOC.md` 作为索引页，使用 Dataview 自动生成列表。

## Dataview 查询

```dataview
TABLE status, date_decided as "决策日期"
FROM "decisions"
WHERE type = "adr" AND status = "proposed"
SORT adr_id DESC
```

**禁止手动维护索引**，全部使用 Dataview 动态生成。
