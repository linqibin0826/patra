patra-spring-boot-starter-provenance 的实现任务清单要点：1）先建模块骨架与包结构（pubmed/、epmc/、common/、boot/），配置 Maven 依赖；2）补齐 common 组件——各类配置 record、DefaultConfigProvider、GatewayRequestBuilder、XmlToJsonConverter、ProvenanceMetrics、ProvenanceClientException；3）实现 PubMed 与 EPMC 客户端及其 Request/Response record、NoOp 降级和 JSON 优先策略；4）完成 Spring Boot 自动配置（条件装配、NoOp 降级、Micrometer 可选）；5）补齐配置文件、README、自动配置清单并通过编译；后续建议：集成到 patra-ingest、补齐单元/集成测试、性能优化与新增数据源。