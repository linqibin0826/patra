<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.patra.ingest.infra.persistence.mapper.TaskRunMapper">

    <!--
        获取任务的最新 attemptNo
        
        用途：生成下一次尝试编号（latestAttemptNo + 1）
        
        返回：最大 attemptNo，若无记录则返回 0
    -->
    <select id="selectLatestAttemptNo" resultType="int">
        SELECT IFNULL(MAX(attempt_no), 0)
          FROM patra_ingest.ing_task_run
         WHERE task_id = #{taskId} and deleted = 0
    </select>

    <!--
        覆盖写入检查点并刷新心跳时间。
        空字符串或 null 表示清空 checkpoint。
    -->
    <update id="updateCheckpointAndHeartbeat">
        UPDATE patra_ingest.ing_task_run
           SET last_heartbeat = #{now},
               updated_at = #{now},
               checkpoint =
               <choose>
                   <when test="checkpointJson != null and checkpointJson != ''">
                       CAST(#{checkpointJson} AS JSON)
                   </when>
                   <otherwise>
                       NULL
                   </otherwise>
               </choose>
         WHERE id = #{runId}
           AND deleted = 0
    </update>

    <!--
        仅刷新心跳时间，不修改 checkpoint。
    -->
    <update id="touchHeartbeat">
        UPDATE patra_ingest.ing_task_run
           SET last_heartbeat = #{now},
               updated_at = #{now}
         WHERE id = #{runId}
           AND deleted = 0
    </update>

    <!--
        将运行记录标记为失败并记录错误信息。
    -->
    <update id="markFailed">
        UPDATE patra_ingest.ing_task_run
           SET status_code = 'FAILED',
               error = #{errorMsg},
               finished_at = #{now},
               last_heartbeat = #{now},
               updated_at = #{now}
         WHERE id = #{runId}
           AND deleted = 0
    </update>

</mapper>
