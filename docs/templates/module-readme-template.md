# 模块文档模板

> 使用说明：将占位符替换为实际信息，必要时删除无关段落。所有说明请使用中文。

## 1. 模块定位
- **服务/组件作用**：`<简述模块在平台中的角色>`
- **主要消费者**：`<列出依赖此模块的服务或场景>`
- **架构边界**：`<说明所属分层及依赖方向约束>`

## 2. 核心能力
- `<能力名称>`：`<能力摘要 + 价值>`
- `<能力名称>`：`<能力摘要 + 价值>`
- `<可按需追加>`

## 3. 分层结构与依赖
- **子模块/包结构**：`<列出 adapter/app/domain/infra 或对应结构>`
- **关键依赖**：`<列出外部组件、Starter、三方库>`
- **禁止事项**：`<列出不允许的依赖或反向调用>`

## 4. 运行与配置
- **引入方式**：`<Maven 依赖片段>`
- **必要配置**：`<YAML 或环境变量示例>`
- **启动步骤**：`<本地运行或部署要点>`

## 5. 观测与运维
- **日志/指标**：`<关键日志字段与 Micrometer 指标>`
- **排障手册**：`<常见问题 + 排查路径>`
- **健康检查**：`<探针/巡检任务/报警规则>`

## 6. 测试策略
- **单元测试重点**：`<列出必须覆盖的领域/算法>`
- **集成测试场景**：`<列出跨模块/外部依赖验证>`
- **测试数据**：`<说明样例或基准数据>`

## 7. Roadmap 与风险
- **近期计划**：`<高/中/低优先级事项>`
- **既有风险**：`<列出潜在风险与缓解方案>`
- **回滚策略**：`<针对关键变更的回滚要点>`

## 8. 参考资料
- `<相关文档或源码链接>`
- `<其他补充>`
